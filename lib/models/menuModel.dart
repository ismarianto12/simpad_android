import 'package:flutter/material.dart';

class menuModel {
  final String? title;
  final String? subtitle;
  final IconData? icon;
  final String? route;
  final Color? colors;

  menuModel({this.title, this.subtitle, this.icon, this.route, this.colors});
}
